# .env.example — document all environment variables used by the occupancy module.
# Copy this file to .env and fill in values as needed.
# Never commit a .env file with real values to version control.

# Directory where output CSV files are written.
# Defaults to outputs/ relative to the project root when unset.
OCCUPANCY_OUTPUT_DIR=outputs/
